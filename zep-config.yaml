api_secret: "dev-secret"
auth:
  required: false
store:
  type: postgres
  postgres:
    dsn: postgres://postgres:password@postgres:5432/engram?sslmode=disable
server:
  listen_address: 0.0.0.0
  port: 8000

